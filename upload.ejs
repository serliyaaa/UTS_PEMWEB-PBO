<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload File</title>
    <link rel="stylesheet" href="/path/to/your/styles.css"> <!-- Ganti dengan path CSS yang sesuai -->
</head>
<style>
    /* Reset dasar untuk menghilangkan margin dan padding default */
* /* Reset dasar untuk menghilangkan margin dan padding default */
* /* Reset dasar untuk menghilangkan margin dan padding default */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Mengatur font dan latar belakang keseluruhan */
body {
    font-family: 'Arial', sans-serif;
    background-color: #e9f5ff; /* Latar belakang biru cerah */
    color: #333;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh; /* Memenuhi tinggi viewport */
}

/* Kontainer untuk form */
.container {
    background-color: #ffffff;
    padding: 40px 30px; /* Padding untuk kontainer */
    border-radius: 10px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    width: 400px; /* Lebar kontainer */
    text-align: center;
}

/* Judul form */
h1 {
    color: #ff6f61; /* Warna coral cerah */
    font-size: 24px; /* Ukuran font untuk judul */
    margin-bottom: 30px; /* Ruang di bawah judul */
    font-family: 'Georgia', serif; /* Font serif untuk judul */
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1); /* Bayangan teks */
}

/* Mengatur label */
label {
    display: block;
    margin: 15px 0 5px;
    font-weight: bold;
    font-size: 16px; /* Ukuran font untuk label */
}

/* Mengatur input dan textarea */
input[type="text"],
textarea,
input[type="file"] {
    width: 100%;
    padding: 12px;
    border: 2px solid #ff6f61; /* Border warna coral */
    border-radius: 5px;
    margin-bottom: 20px; /* Ruang di bawah input */
    font-size: 14px; /* Ukuran font untuk input */
    transition: border-color 0.3s; /* Transisi border saat fokus */
}

/* Mengatur input saat fokus */
input[type="text"]:focus,
textarea:focus,
input[type="file"]:focus {
    border-color: #ff3b30; /* Warna border saat fokus */
    outline: none; /* Menghilangkan outline default */
}

/* Mengatur tombol submit */
button {
    background-color: #ff6f61; /* Warna coral */
    color: white;
    padding: 12px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
    transition: background-color 0.3s; /* Transisi saat hover */
    width: 100%; /* Tombol memenuhi lebar kontainer */
}

/* Mengatur tombol saat hover */
button:hover {
    background-color: #ff3b30; /* Warna coral lebih gelap saat hover */
}

/* Menambahkan sedikit ruang di bagian bawah */
form {
    margin-top: 20px;
}

</style>
<body>
    <h1>Upload File</h1>
    <form action="/upload" method="POST" enctype="multipart/form-data">
        <label for="name">Nama:</label>
        <input type="text" name="name" required>
        <br>
        <label for="description">Keterangan:</label>
        <textarea name="description" required></textarea>
        <br>
        <label for="file">Pilih file:</label>
        <input type="file" name="file" required>
        <br>
        <button type="submit">Upload</button>
    </form>
</body>
</html>
